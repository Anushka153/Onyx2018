<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowExitTransition">@transition/list_exit</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="actionBarSize">50dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="navTextStyle" parent="Base.TextAppearance.AppCompat">
        <item name="android:textColor">#644a4a</item>
        <item name="textSize">16sp</item>
        <item name="android:textColorHighlight">#ffffff</item>
        <item name="android:textStyle">italic|bold</item>
        <item name="android:fontFamily">serif</item>
    </style>

</resources>
